{% extends "base.twig" %}

{% block content %}
	<div class="container">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<h1>{{post.title}}</h1>
        {{post.content}}
			</section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}
